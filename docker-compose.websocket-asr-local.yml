services:
  websocket-asr-local:
    build:
      context: .
      dockerfile: Dockerfile.websocket-asr-local
    container_name: websocket-asr-local
    ports:
      - "8765:8765"
    volumes:
      - ./models:/app/models:ro  # Mount models directory as read-only
    restart: unless-stopped